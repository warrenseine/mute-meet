{
  "name": "mute-meet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild server.js --bundle --platform=node --format=cjs --external:node:* --outfile=dist/mute-meet.cjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
